runtime configuration statements

```sg
#[configure("name_of_configuration", value)]
// example:
#[configure("dev_preserve_expired_frames", true)]
```

other operators (and, or, xor...)

control flow (if/else statements, loops...)

- [x] If statements
- [x] While loops
- [x] Unconditional loops
- [ ] For loops

struct impl blocks

type casting

bitwise operations

include statements

- i'd also like some functions to run only when they're configured to upon load, perhaps to initialise values or something

arrays
